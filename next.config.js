/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["avatars.githubusercontent.com", "cloudflare-ipfs.com", "6huyzizusvkpcoai.public.blob.vercel-storage.com"]
    }
}

module.exports = nextConfig
